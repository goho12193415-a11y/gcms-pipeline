# ---- Python ----
__pycache__/
*.pyc

# ---- Outputs & scratch ----
output/
_mzml_tmp/

# ---- Raw / processed MS data (large; not for version control) ----
*.raw
*.mzML
*.msp

# ---- NIST-derived databases ----
# Derived from the commercial NIST library / NIST ri.dat — do NOT redistribute
# publicly (license), and nist_gcms.json exceeds GitHub's 100 MB limit anyway.
library/
*.bak

# ---- CNN training data (large) ----
cnnPolar.txt

# ---- Thermo proprietary binaries (do not redistribute) ----
thermo_lib/

# ---- Source-material archives (may contain NIST-derived data) ----
*.zip

# ---- Third-party code — NOT ours, do not publish (license/attribution) ----
# Matyushin 2021 RI-prediction model (Java/Maven) + legacy Lazarus GUI.
# Kept locally (and in _thirdparty_backup/) but never tracked/redistributed.
_thirdparty_backup/
*.java
*.pas
*.lfm
*.lpr
*.lpi
*.ico
pom*.xml
compile.sh
