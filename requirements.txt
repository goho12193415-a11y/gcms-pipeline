# GC-MS pipeline — pinned to the versions this project is known to run on.
# (Re)install into a Python 3.11 environment with:
#     pip install -r requirements.txt
#
# Core numerics / IO
numpy==1.26.4
scipy==1.16.3
pandas==2.3.1
openpyxl==3.1.5

# Mass-spec formats
pymzml==2.6.1          # .mzML reader
olefile==0.47          # Shimadzu .qgd (OLE2 container) reader

# NIST search + native Thermo .RAW (Windows only)
pyms-nist-search==0.8.0   # NIST MS Search DLL wrapper
pythonnet==3.1.0          # loads Thermo RawFileReader.dll via coreclr
